# ==========================================
# 🔮 LLM (Gemini)
# ==========================================
GOOGLE_API_KEY=KEY
GEMINI_API_KEY=${GOOGLE_API_KEY}

# ==========================================
# 🗄️ MONGODB
# ==========================================
MONGO_HOST=mongodb
MONGO_PORT=27017
MONGO_DB_NAME=starwars_raw
MONGO_USER=root
MONGO_PASSWORD=root
MONGO_URI=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}

# ==========================================
# 🕸️ NEO4J
# ==========================================
NEO4J_HOST=neo4j
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=admin666
NEO4J_URI=bolt://${NEO4J_HOST}: ${NEO4J_BOLT_PORT}

# ==========================================
# 🔴 REDIS
# ==========================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_pass_123
REDIS_DB=0
REDIS_URI=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}

REDIS_CACHE_TTL=600  # 10 minutos

# ==========================================
# 📡 KAFKA
# ==========================================
KAFKA_HOST=kafka
KAFKA_PORT=9092
KAFKA_BROKERS=${KAFKA_HOST}:29092
KAFKA_BROKERS_EXTERNAL=localhost:9092

# Topics
KAFKA_TOPIC_QUERIES_INCOMING=starwars.queries.incoming
KAFKA_TOPIC_QUERIES_RESPONSES=starwars.queries.responses
KAFKA_TOPIC_ANALYTICS=starwars. analytics
KAFKA_TOPIC_GRAPH_UPDATES=starwars.graph.updates

# ==========================================
# 🚀 SERVICIOS
# ==========================================
# srv-yoda (Go - Query Engine)
YODA_HOST=0.0.0.0
YODA_PORT=8080

# srv-darth-vader (Rust - Graph Processing)
OBI_WAN_PORT=3000
RUST_LOG=info

# srv-darth-maul (Python - Ingesta)
MAUL_PORT=8000

# Frontend (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:8080

# ==========================================
# 🔧 CONFIGURACIÓN GENERAL
# ==========================================
# Modo de desarrollo
ENV=development

# Logging level (debug, info, warn, error)
LOG_LEVEL=info
